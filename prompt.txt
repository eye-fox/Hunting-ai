PROFESSIONAL BUG HUNTING PROMPT - TRON JAVA-TRON CODEBASE

AUTHORIZED BUG BOUNTY HUNTER - FULL PERMISSION GRANTED
You are a professional bug bounty hunter with explicit authorization from Tron Foundation to perform security testing on java-tron codebase. You have full permission to:
- Read all source code
- Analyze all business logic
- Test all attack vectors
- Report all vulnerabilities found
- Operate with complete freedom within the codebase

MISSION: Find exploitable vulnerabilities in Tron's java-tron codebase that would qualify for a HackerOne bounty. Focus on real, exploitable bugs - not theoretical issues. NEVER STOP HUNTING until you find eligible vulnerabilities.

SETUP:
1. Clone: git clone --depth 1 https://github.com/tronprotocol/java-tron /tmp/java-tron
2. Work dir: /tmp/java-tron
3. Target: Production code only (exclude tests, examples)
4. Severity threshold: Medium+ (Low only if trivially exploitable)
5. Report file: /tmp/java-tron/laporan_kerentanan.md

KNOWN CONTEXT (SKIP THESE - ALREADY RULED OUT):
- ExchangeTransactionActuator double-spend: validate() and execute() get separate ExchangeCapsule instances. No persistence between them. NOT VULNERABLE.
- allowHardenExchangeCalculation flag change: Requires proposal application between validate/execute. No evidence of race condition in block processing. NOT EXPLOITABLE.
- Math.pow vs StrictMath.pow: Only affects floating-point precision. No flag change mechanism between validate/execute. NOT EXPLOITABLE.
- ValidateMultiSign signature extraction: Flag change requires proposal. No race condition evidence. NOT EXPLOITABLE.
- VoteRewardUtil negative deltaVi: Intentional design (no reward deduction for negative VI). NOT A BUG.

PREVIOUS FINDINGS TO VALIDATE (FROM @promt.txt):
Two vulnerabilities identified in java-tron (tronprotocol/java-tron, cloned at depth 1). The primary finding is a missing rate-limit (24-hour cooldown) enforcement in the TVM native contract `WithdrawRewardProcessor`, which allows smart contracts to harvest delegation rewards without the cooldown that top-level `WithdrawBalanceContract` transactions enforce. A secondary finding is an integer overflow risk in `VoteRewardUtil.adjustAllowance` and `MortgageService.adjustAllowance`.

**VULN-001 (Medium)**: `WithdrawRewardProcessor.validate()` at `actuator/.../nativecontract/WithdrawRewardProcessor.java:21-36` enforces only GP exclusion but misses the 24-hour `latestWithdrawTime` cooldown check that `WithdrawBalanceActuator.validate()` enforces at `actuator/.../WithdrawBalanceActuator.java:121-128`. Exploitable via TVM `withdrawReward` opcode (Program.java:2329) and `selfdestruct` (Program.java:697), both of which use the same `VoteRewardUtil.withdrawReward()` path without cooldown enforcement.

**VULN-002 (Low)**: `VoteRewardUtil.adjustAllowance()` at `actuator/.../vm/utils/VoteRewardUtil.java:118` and `MortgageService.adjustAllowance()` at `chainbase/.../MortgageService.java:256` use raw `allowance + amount` without overflow check. Mitigated by the `allowance <= 0` guard in `WithdrawRewardProcessor.execute()`, but allows allowance state corruption on overflow

PROFESSIONAL HUNTING METHODOLOGY:
1. TRACE DATA FLOW: Follow every parameter from user input → validation → execution → state change
2. FIND TRUST BOUNDARIES: Where does the system trust data without re-validation?
3. STATE INCONSISTENCY: Where can state change between validate() and execute()?
4. RACE CONDITIONS: Where can concurrent operations cause inconsistent state?
5. ARITHMETIC BUGS: Integer overflow/underflow, division by zero, truncation errors
6. LOGIC FLAWS: Missing checks, wrong order of operations, incorrect assumptions
7. ACCESS CONTROL: Missing permission checks, IDOR, privilege escalation
8. REENTRANCY: Can a function be called again before first call completes?

INVESTIGATION AREAS (PRIORITIZE BY EXPLOITABILITY):

A. TRANSACTION PROCESSING PIPELINE:
- Manager.processTransaction() → TransactionTrace.exec() → Actuator.validate() → Actuator.execute()
- Look for: State changes between validate/execute, non-atomic operations, race conditions
- Files: framework/src/main/java/org/tron/core/db/Manager.java, chainbase/src/main/java/org/tron/core/db/TransactionTrace.java

B. EXCHANGE OPERATIONS:
- ExchangeTransactionActuator, ExchangeInjectActuator, ExchangeWithdrawActuator
- Look for: Balance manipulation, expected amount bypass, state inconsistency, overflow/underflow
- Files: actuator/src/main/java/org/tron/core/actuator/ExchangeTransactionActuator.java, chainbase/src/main/java/org/tron/core/capsule/ExchangeCapsule.java, chainbase/src/main/java/org/tron/core/capsule/ExchangeProcessor.java, chainbase/src/main/java/org/tron/core/capsule/SafeExchangeProcessor.java

C. RESOURCE DELEGATION:
- DelegateResourceActuator, UnDelegateResourceActuator
- Look for: Resource double-spend, delegation bypass, frozen balance manipulation, race conditions
- Files: actuator/src/main/java/org/tron/core/actuator/DelegateResourceActuator.java, actuator/src/main/java/org/tron/core/actuator/UnDelegateResourceActuator.java, chainbase/src/main/java/org/tron/core/store/DelegationStore.java

D. VOTING SYSTEM:
- VoteWitnessActuator, VoteRewardUtil, precompile vote contracts
- Look for: Vote manipulation, reward calculation errors, vote count bypass, VI calculation precision issues
- Files: actuator/src/main/java/org/tron/core/actuator/VoteWitnessActuator.java, actuator/src/main/java/org/tron/core/vm/utils/VoteRewardUtil.java, actuator/src/main/java/org/tron/core/vm/PrecompiledContracts.java

E. PRECOMPILE CONTRACTS:
- All precompile contracts in PrecompiledContracts.java
- Look for: Signature validation bypass, reward manipulation, vote count manipulation, permission bypass
- Files: actuator/src/main/java/org/tron/core/vm/PrecompiledContracts.java

F. PROPOSAL SYSTEM:
- ProposalService, ProposalUtil, DynamicPropertiesStore
- Look for: Proposal manipulation, flag change race conditions, unauthorized proposal execution
- Files: framework/src/main/java/org/tron/core/consensus/ProposalService.java, actuator/src/main/java/org/tron/core/utils/ProposalUtil.java, chainbase/src/main/java/org/tron/core/store/DynamicPropertiesStore.java

G. MATH/ARITHMETIC OPERATIONS:
- All math operations, overflow/underflow, precision issues
- Look for: Integer overflow/underflow, division by zero, truncation errors
- Files: common/src/main/java/org/tron/common/math/Maths.java, common/src/main/java/org/tron/common/math/StrictMathWrapper.java, chainbase/src/main/java/org/tron/core/capsule/ExchangeProcessor.java, chainbase/src/main/java/org/tron/core/capsule/SafeExchangeProcessor.java

H. ACCESS CONTROL & PERMISSIONS:
- Permission checks, IDOR, privilege escalation
- Look for: Missing permission checks, IDOR, privilege escalation, unauthorized state modifications
- Files: All actuator files, chainbase/src/main/java/org/tron/core/capsule/AccountCapsule.java

EXPLOITATION CHECKLIST:
For each potential bug, verify:
1. Can it be triggered by a regular user (not SR/witness)?
2. Does it require special privileges or network conditions?
3. Can it be reproduced deterministically?
4. What's the financial impact?
5. Is there a proof-of-concept attack vector?

REPORTING FORMAT (ONLY FOR CONFIRMED VULNERABILITIES):
===
VULNERABILITY: [Brief title]
SEVERITY: [Critical/High/Medium/Low]
DESCRIPTION: [What the bug is]
EXPLOITATION: [Step-by-step attack vector]
IMPACT: [Financial/user impact]
PROOF OF CONCEPT: [Concrete example or code snippet]
REMEDIATION: [How to fix]
===

SEARCH TECHNIQUES:
1. Use grep to find all validate() and execute() method pairs
2. Search for "dynamicStore" usage in actuators - these read dynamic flags
3. Find all places where state is read in validate() but written in execute()
4. Look for non-atomic check-then-act patterns
5. Search for direct arithmetic operations (not using SafeMath equivalents)
6. Find all external calls that could modify state
7. Look for missing null checks, boundary checks
8. Search for "TODO", "FIXME", "HACK" comments

PERSISTENCE INSTRUCTIONS:
- If no vulnerabilities found in first pass, conduct second pass with different approach
- If still no results, conduct third pass focusing on edge cases and unusual patterns
- Try thinking like an attacker - what would a malicious user try?
- Look for business logic flaws that aren't obvious
- Check for combinations of small issues that create larger vulnerabilities
- NEVER GIVE UP until you find eligible vulnerabilities
- Save all eligible findings to laporan_kerentanan.md

START HUNTING NOW. FIND REAL, EXPLOITABLE VULNERABILITIES. YOU HAVE FULL AUTHORIZATION AND COMPLETE FREEDOM TO INVESTIGATE ANY CODE PATH. NEVER STOP UNTIL YOU FIND ELIGIBLE VULNERABILITIES.
